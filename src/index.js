const path = require('path');
const images = require('images');
const imageInfo = require("imageinfo");
const xlsx = require('node-xlsx');

// 读取文件夹下的图片
function readPhotoList(path){

}

// 读取excel文件
function readExcel(path){

}

// 开始生成水印
function generateWatermark(){

}